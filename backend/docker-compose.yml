version: "3.9"
services:
  backend:
    build: .
    container_name: smart-pdf-backend
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    env_file:
      - .env.example
    volumes:
      - ./app:/app/app
      - ./tmp:/app/tmp
    restart: unless-stopped
